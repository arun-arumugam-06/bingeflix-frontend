{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "postinstall": "chmod +x node_modules/.bin/vite || true"
  },
  "dependencies": {
    "vite": "^7.0.6",
    "@vitejs/plugin-react-swc": "^3.11.0"
  }
}
